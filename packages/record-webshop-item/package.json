{
  "name": "@centralbeans/record-webshop-item",
  "type": "module",
  "scripts": {
    "build": "cp .yarnrc-lambda.yml .yarnrc.yml && yarn workspaces focus --production && zip -rq package.zip . -x .yarnrc.yml .yarnrc-lambda.yml && rm .yarnrc.yml",
    "deploy": "aws lambda update-function-code --function-name recordWebshopItem --zip-file fileb://package.zip --publish",
    "test": "node test.js"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.1069.0",
    "@sentry/aws-serverless": "^10.58.0"
  }
}
