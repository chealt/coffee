{
  "name": "@centralbeans/record-webshop-item-details",
  "type": "module",
  "scripts": {
    "build": "cp .yarnrc-lambda.yml .yarnrc.yml && yarn workspaces focus --production && zip -rq package.zip . -x .yarnrc.yml .yarnrc-lambda.yml && rm .yarnrc.yml",
    "upload": "aws s3 cp package.zip s3://centralbeans-artifacts/record-webshop-item-details/",
    "update": "aws lambda update-function-code --function-name recordWebshopItemDetails --s3-bucket centralbeans-artifacts --s3-key record-webshop-item-details/package.zip --publish",
    "version": "aws lambda list-versions-by-function --function-name recordWebshopItemDetails --query 'Versions[?Version!=`$LATEST`] | [-1]'",
    "deploy": "yarn upload > /dev/null && yarn update > /dev/null && yarn run version"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1073.0",
    "@aws-sdk/client-s3": "^3.1073.0",
    "@aws-sdk/client-secrets-manager": "^3.1073.0",
    "@libsql/client": "^0.17.4",
    "@sentry/aws-serverless": "^10.59.0",
    "sharp": "^0.35.2"
  }
}
