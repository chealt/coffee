---
import T from "@components/i18n/t.astro";
import { getTranslator } from "@components/i18n/t.js";
import Common from "../common/common.astro";

const t = await getTranslator({
  filePath: "common",
  locale: Astro.currentLocale,
});
---

<Common>
  <h1><T namespace="common.locales" key="500title">{t("500title")}</T></h1>
  <section><T namespace="common.locales" key="500">{t("500")}</T></section>
</Common>
