---
import { getRelativeLocaleUrl } from "astro:i18n";

import T from "@components/i18n/t.astro";
import { getTranslator } from "@components/i18n/t.js";
import Button from "../form/button/button.astro";
import coffees from "../../../data/coffees.json" with { type: "json" };
import Common from "../common/common.astro";
import Coffees from "./list.astro";
import Icon from "../icon/icon.astro";

const t = await getTranslator({
  filePath: "coffees",
  locale: Astro.currentLocale,
});
const tc = await getTranslator({
  filePath: "common",
  locale: Astro.currentLocale,
});

const { loggedInUser } = Astro.locals;
---

<Common>
  <h1 class="title">
    <div><T namespace="coffees.locales" key="coffees">{t("coffees")}</T></div>
    {
      loggedInUser && (
        <Button
          class="ghost small"
          href={getRelativeLocaleUrl(Astro.currentLocale, `/coffees/stats`)}
        >
          <Icon type="chart" />
          <span class="visually-hidden"><T namespace="common.locales" key="chart">{tc("chart")}</T></span>
        </Button>
      )
    }
  </h1>
  <section>
    <Coffees coffees={coffees} />
  </section>
</Common>
