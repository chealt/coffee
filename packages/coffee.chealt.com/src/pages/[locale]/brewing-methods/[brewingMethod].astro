---
export const prerender = false;

import BrewingMethods from '../../../components/brewing-methods/brewing-method.astro';
import brewingMethods from '../../../../data/brewingMethods.json';

const { brewingMethod } = Astro.params;

const id = brewingMethods.find(({ name }) => name === brewingMethod)?.brewing_method_id;

Astro.response.headers.set('Cache-Control', 'public, max-age=86400');
---

<BrewingMethods id={id} />
