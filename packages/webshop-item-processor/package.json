{
  "name": "@chealt/webshop-item-processor",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && mkdir dist/data && cp ../coffee.chealt.com/data/*.json dist/data && cp -r package.json dist && cp ../../yarn.lock dist && cp .yarnrc-lambda.yml dist/.yarnrc.yml && cp -r src dist && cd dist && yarn workspaces focus && rm -rf .yarn && rm -rf yarn.lock && rm -rf .yarnrc.yml && zip -rq package.zip .",
    "deploy": "aws lambda update-function-code --function-name webshopItemProcessor --zip-file fileb://dist/package.zip --publish",
    "test": "rm -rf data && mkdir data && cp ../coffee.chealt.com/data/*.json data && node --env-file=../coffee-admin/.env test.js"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.962.0",
    "@aws-sdk/client-translate": "^3.962.0",
    "jsdom": "27.3.0"
  },
  "resolutions": {
    "parse5": "7.3.0"
  }
}
