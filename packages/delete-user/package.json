{
  "name": "@centralbeans/delete-user",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r package.json dist && cp ../../yarn.lock dist && cp .yarnrc-lambda.yml dist/.yarnrc.yml&& cp -r src dist && cd dist && yarn workspaces focus && rm -rf .yarn && rm -rf yarn.lock && rm -rf .yarnrc.yml && zip -rq package.zip .",
    "deploy": "aws lambda update-function-code --function-name deleteUser --zip-file fileb://dist/package.zip --publish"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.962.0",
    "@libsql/client": "^0.15.15",
    "@sentry/aws-serverless": "^10.32.1",
    "@tursodatabase/api": "^1.9.2"
  }
}
