{
  "name": "@centralbeans/webshop-processor",
  "type": "module",
  "scripts": {
    "build": "cp .yarnrc-lambda.yml .yarnrc.yml && mkdir -p data && cp ../coffee.chealt.com/data/*.json data && yarn workspaces focus --production && zip -rq package.zip . -x .yarnrc.yml .yarnrc-lambda.yml && rm .yarnrc.yml && rm -rf data",
    "deploy": "aws lambda update-function-code --function-name webshopProcessor --zip-file fileb://package.zip --publish",
    "test": "node --env-file=../coffee-admin/.env test.js"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.1069.0",
    "@sentry/aws-serverless": "^10.58.0",
    "jsdom": "27.0.0"
  }
}
