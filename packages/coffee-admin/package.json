{
  "name": "@chealt/coffee-admin",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.js dist && cp -r package.json dist && cp ../../yarn.lock dist && cp .yarnrc.yml dist && cp -r src dist && cd dist && yarn workspaces focus && rm -rf .yarn && rm -rf yarn.lock && rm -rf .yarnrc.yml && zip -rq package.zip .",
    "users:create": "node --env-file=.env src/users/create-user.js",
    "users:send-registration-code": "node --env-file=.env src/users/send-registration-code.js",
    "coffees:flag-removed": "node --env-file=.env src/coffees/flag-removed.js",
    "coffees:import": "node --env-file=.env src/coffees/import.js",
    "coffees:import:CQI": "node --env-file=.env ../../node_modules/playwright/cli.js test",
    "coffees:import:varieties": "node --env-file=.env src/coffees/varieties.js",
    "currencies:import": "node --env-file=src/currencies/.env src/currencies/import.js",
    "taste-notes:copy-i18n": "node --env-file=.env src/taste-notes/copy-i18n.js",
    "taste-notes:translate": "node --env-file=.env src/taste-notes/translate.js"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.913.0",
    "@aws-sdk/client-sesv2": "^3.911.0",
    "@aws-sdk/client-translate": "^3.911.0",
    "@libsql/client": "^0.15.15",
    "@playwright/test": "^1.56.1",
    "@tursodatabase/api": "^1.9.0",
    "jsdom": "^27.0.0",
    "jsonwebtoken": "^9.0.2",
    "undici": "^7.16.0"
  }
}
