{
  "name": "@chealt/image-processor",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r package.json dist && cp ../../yarn.lock dist && cp .yarnrc-lambda.yml dist/.yarnrc.yml&& cp -r src dist && cd dist && yarn workspaces focus && rm -rf .yarn && rm -rf yarn.lock && rm -rf .yarnrc.yml && zip -rq package.zip .",
    "deploy": "aws lambda update-function-code --function-name imageProcessor --zip-file fileb://dist/package.zip --publish"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.933.0",
    "@aws-sdk/client-s3": "^3.932.0",
    "@aws-sdk/client-textract": "^3.932.0",
    "sharp": "^0.34.5"
  }
}
