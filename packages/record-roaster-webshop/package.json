{
  "name": "@centralbeans/record-roaster-webshop",
  "type": "module",
  "scripts": {
    "build": "cp .yarnrc-lambda.yml .yarnrc.yml && mkdir -p data && cp ../coffee.chealt.com/data/roasters.json data/roasters.json && yarn workspaces focus --production && zip -rq package.zip . -x .yarnrc.yml .yarnrc-lambda.yml && rm .yarnrc.yml",
    "deploy": "aws lambda update-function-code --function-name recordRoasterWebshop --zip-file fileb://package.zip --publish"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.994.0",
    "@sentry/aws-serverless": "^10.39.0"
  }
}
