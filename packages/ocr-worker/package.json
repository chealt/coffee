{
  "name": "@chealt/ocr-worker",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start:CLI": "node ./src/CLI.js",
    "deploy": "aws s3 cp OCRWorker.zip s3://ocr-worker/OCRWorker.zip",
    "build": "rm -rf dist && mkdir dist && cp -r package.json dist && cp ../../yarn.lock dist && cp .yarnrc.yml dist && cp -r src/ dist && cp -r models dist && cd dist && yarn workspaces focus && rm -rf .yarn && rm -rf yarn.lock && rm -rf .yarnrc.yml && zip -rq ../OCRWorker.zip ."
  },
  "dependencies": {
    "@techstark/opencv-js": "^4.11.0-release.1",
    "js-clipper": "^1.0.1",
    "onnxruntime-web": "^1.23.0",
    "sharp": "^0.34.4",
    "tiny-invariant": "^1.3.3"
  }
}
